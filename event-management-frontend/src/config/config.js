// Configuration settings for the Tech Event Hub application

const config = {
  apiUrl: import.meta.env.VITE_API_URL,
  tokenKey: import.meta.env.VITE_TOKEN_KEY,
  userKey: import.meta.env.VITE_USER_KEY,
};

export default config;
